# Whey

Whey is a backend registry service that mirrors GitHub-released packages for Parm and provides additional metadata and analytics as well as cache expensive operations that don't need to be performed client-side.

